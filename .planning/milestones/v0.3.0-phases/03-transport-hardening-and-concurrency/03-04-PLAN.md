---
plan: 03-04
phase: 3
wave: 3
depends_on: [03-01]
files_modified:
  - src/domain/classification.rs
  - src/domain/mod.rs
  - docs/OPERATION-CLASSIFICATION.md
  - tests/classification_invariants.rs
autonomous: true
requirements: [TRANS-07]
---

# Plan 03-04: Documented Read-Only vs Destructive Operation Classification

<objective>
为全部 37 个 JSON-RPC 方法建立只读/破坏性分类，使破坏性操作（删除容器、导入密钥/证书、锁定设备、设置标签等）在代码与文档中可区分。本阶段只分类与文档，不据分类做强制（03-CONTEXT D-20/D-21/D-22）。

代码是唯一真源：`OperationClass` 枚举 + 全量映射 + 覆盖测试；文档表格引用代码分类。
</objective>

<threat_model>
**ASVS Level:** 1 · **Block on:** high

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-03-14 | 新增方法漏分类，未来基于分类的安全控制出现盲点 | MEDIUM | 不变量测试断言分类集合与 `EXPECTED_METHODS` 双向一致 |
| T-03-15 | 分类被误用于强制从而改变 v0.2.0 行为 | MEDIUM | 本阶段分类不接入任何请求路径；仅枚举、映射、文档与测试 |
| T-03-16 | 分类漂移（同一方法在不同位置不同类） | LOW | 单一映射常量，重复名断言失败 |
</threat_model>

<must_haves>
- `src/domain/classification.rs` 定义 `pub enum OperationClass { ReadOnly, Destructive }`
- 一个常量映射覆盖全部 37 个 `EXPECTED_METHODS` 方法，且无重复
- 不变量测试断言分类名集合与 `tests/common::EXPECTED_METHODS` 完全一致
- `docs/OPERATION-CLASSIFICATION.md` 列出每个方法的分类并注明代码为真源
- 分类不接入任何请求处理路径
</must_haves>

<tasks>

<task id="03-04-01" type="execute">
  <name>定义 OperationClass 与 37 方法映射</name>
  <files>src/domain/classification.rs, src/domain/mod.rs</files>
  <read_first>
    - `tests/common/mod.rs` 的 `EXPECTED_METHODS`（37 个方法的权威列表）
    - `src/domain/mod.rs`（模块声明）
    - `.planning/phases/03-transport-hardening-and-concurrency/03-CONTEXT.md` — D-20
  </read_first>
  <action>
    1. 新建 `src/domain/classification.rs`：
       ```rust
       //! Read-only vs destructive classification for every JSON-RPC method.
       //!
       //! This phase classifies and documents only; nothing here is consulted on a
       //! request path. A later phase may use it to gate authorization or audit.
       #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
       pub enum OperationClass { ReadOnly, Destructive }
       impl OperationClass { pub fn as_str(self) -> &'static str { ... } }

       /// Every method the service exposes, with its class.
       /// Kept in sync with `tests/common::EXPECTED_METHODS` by an invariant test.
       pub const CLASSIFIED_METHODS: &[(&str, OperationClass)] = &[
           ("SetLanguage", OperationClass::ReadOnly),
           ("WaitForDevEvent", OperationClass::ReadOnly),
           ("EnumProvider", OperationClass::ReadOnly),
           ("EnumDevice", OperationClass::ReadOnly),
           ("ConnectDev", OperationClass::Destructive),
           ("EnumApplication", OperationClass::ReadOnly),
           ("EnumContainer", OperationClass::ReadOnly),
           ("DeleteContainer", OperationClass::Destructive),
           ("IssueCertificate", OperationClass::Destructive),
           ("ImportCertificate", OperationClass::Destructive),
           ("SignData", OperationClass::ReadOnly),
           ("DisConnectDev", OperationClass::Destructive),
           ("FindCertificates", OperationClass::ReadOnly),
           ("GenerateRandom", OperationClass::ReadOnly),
           ("Digest", OperationClass::ReadOnly),
           ("CheckPIN", OperationClass::Destructive),
           ("CreatePKCS10", OperationClass::Destructive),
           ("EncryptData", OperationClass::ReadOnly),
           ("DecryptData", OperationClass::ReadOnly),
           ("GetDevInfo", OperationClass::ReadOnly),
           ("GetDevState", OperationClass::ReadOnly),
           ("SetLabel", OperationClass::Destructive),
           ("ECCVerify", OperationClass::ReadOnly),
           ("CreateContainer", OperationClass::Destructive),
           ("GetContainerType", OperationClass::ReadOnly),
           ("RSASignData", OperationClass::ReadOnly),
           ("LockDev", OperationClass::Destructive),
           ("UnlockDev", OperationClass::Destructive),
           ("Transmit", OperationClass::Destructive),
           ("CancelWaitForDevEvent", OperationClass::ReadOnly),
           ("GenECCKeyPair", OperationClass::Destructive),
           ("GenRSAKeyPair", OperationClass::Destructive),
           ("RSAVerify", OperationClass::ReadOnly),
           ("DigestInit", OperationClass::ReadOnly),
           ("DigestUpdate", OperationClass::ReadOnly),
           ("DigestFinal", OperationClass::ReadOnly),
           ("CloseHash", OperationClass::ReadOnly),
       ];

       pub fn classify(method: &str) -> Option<OperationClass> {
           CLASSIFIED_METHODS.iter().find(|(m, _)| *m == method).map(|(_, c)| *c)
       }
       ```
       归类口径（写入模块注释）：`Destructive` = 改变设备/令牌持久状态、消耗 PIN 重试计数、生成或导入密钥材料、打开/关闭原生资源、改变锁状态；`ReadOnly` = 纯查询与无状态计算（`SignData`/`EncryptData`/`GenerateRandom` 不改变持久状态）。
    2. 在 `src/domain/mod.rs` 增加 `pub mod classification;`。
    3. 在 `classification.rs` 增加 `#[cfg(test)] mod tests`：
       - `classifying_a_known_method_returns_its_class`（抽查 `DeleteContainer` → `Destructive`、`EnumDevice` → `ReadOnly`）；
       - `an_unknown_method_returns_none`。
  </action>
  <acceptance_criteria>
    - `test -f src/domain/classification.rs` 为真
    - `grep -c 'pub enum OperationClass' src/domain/classification.rs` 输出 `1`
    - `grep -c 'CLASSIFIED_METHODS' src/domain/classification.rs` 输出 `>= 2`
    - `grep -c 'pub mod classification' src/domain/mod.rs` 输出 `1`
    - `cargo test classification` 退出码 0
    - `cargo check` 退出码 0
  </acceptance_criteria>
  <verify>`cargo test classification && cargo check`</verify>
</task>

<task id="03-04-02" type="execute">
  <name>分类覆盖不变量测试</name>
  <files>tests/classification_invariants.rs</files>
  <read_first>
    - `tests/common/mod.rs` 的 `EXPECTED_METHODS`（37 个方法）
    - `src/domain/classification.rs`（03-04-01 产出）
    - `tests/session_invariants.rs`（不变量测试风格参考）
  </read_first>
  <action>
    新建 `tests/classification_invariants.rs`（`mod common;`）：
    1. `every_expected_method_is_classified`：
       - 用 `common::EXPECTED_METHODS` 遍历，断言 `skf_service::domain::classification::classify(m)` 为 `Some(_)`；对未分类方法收集后一次性断言为空并打印缺失列表。
    2. `no_method_is_classified_twice`：
       - 遍历 `CLASSIFIED_METHODS`，用 `HashSet` 检测重复名（重复即 panic 并打印重复项）。
    3. `the_classified_set_equals_the_expected_set`：
       - 断言两侧集合完全相等（防止分类表多出未知方法）。
    4. （可选）`destructive_methods_are_the_documented_set`：断言 `DeleteContainer`、`ImportCertificate`、`LockDev`、`SetLabel` 均为 `Destructive`。
  </action>
  <acceptance_criteria>
    - `test -f tests/classification_invariants.rs` 为真
    - `grep -c 'fn every_expected_method_is_classified' tests/classification_invariants.rs` 输出 `1`
    - `grep -c 'fn no_method_is_classified_twice' tests/classification_invariants.rs` 输出 `1`
    - `grep -c 'fn the_classified_set_equals_the_expected_set' tests/classification_invariants.rs` 输出 `1`
    - `cargo test --test classification_invariants` 退出码 0
  </acceptance_criteria>
  <verify>`cargo test --test classification_invariants`</verify>
</task>

<task id="03-04-03" type="execute">
  <name>分类文档</name>
  <files>docs/OPERATION-CLASSIFICATION.md</files>
  <read_first>
    - `src/domain/classification.rs`（真源）
    - `.planning/phases/03-transport-hardening-and-concurrency/03-CONTEXT.md` — D-20/D-22
    - `README.md` / `docs/`（文档风格参考）
  </read_first>
  <action>
    新建 `docs/OPERATION-CLASSIFICATION.md`：
    1. 标题与说明：本表由 `src/domain/classification.rs::CLASSIFIED_METHODS` 生成/维护，代码是唯一真源；`tests/classification_invariants.rs` 保证与 `EXPECTED_METHODS` 一致。
    2. 一节说明归类口径（见 03-04-01）。
    3. 一张表：`Method | Class | Why`，覆盖全部 37 个方法；`Why` 用一句中文说明。
    4. 明确声明：本阶段不据分类做强制（不做授权/审计/确认），留待后续阶段。
  </action>
  <acceptance_criteria>
    - `test -f docs/OPERATION-CLASSIFICATION.md` 为真
    - `grep -c '| SetLabel |' docs/OPERATION-CLASSIFICATION.md` 输出 `1`
    - `grep -c '| DeleteContainer |' docs/OPERATION-CLASSIFICATION.md` 输出 `1`
    - `grep -c 'Destructive' docs/OPERATION-CLASSIFICATION.md` 输出 `>= 16`
    - 文档中的方法行数 ≥ 37（`grep -c '^| [A-Z]' docs/OPERATION-CLASSIFICATION.md` 输出 `>= 37`）
  </acceptance_criteria>
  <verify>`grep -c '^| [A-Z]' docs/OPERATION-CLASSIFICATION.md`</verify>
</task>

</tasks>

<verification>
```bash
cargo test classification
cargo test --test classification_invariants
cargo test
cargo check --all-targets
```
</verification>

<output_spec>
完成后产出 `03-04-SUMMARY.md`，包含：37 方法分类计数（ReadOnly/Destructive）、覆盖测试输出、以及"分类未接入请求路径"的核对结论。
</output_spec>
