---
milestone: v0.3.0
audited: 2026-09-12
status: tech_debt
scores:
  requirements: 44/44
  phases: 6/6
  integration: verified
  flows: 3/5 automated (2 require Windows/CI runtime)
gaps:
  requirements: []
  integration: []
  flows:
    - name: "Windows service install/upgrade/uninstall lifecycle"
      status: "human_verification_pending"
      evidence: "05-HUMAN-UAT.md; packaging/windows/verify-service.ps1 not executed (macOS host, no pwsh)"
    - name: "Tag-triggered Windows release with checksum + extraction verification"
      status: "human_verification_pending"
      evidence: "06-HUMAN-UAT.md; release-windows.yml not executed (needs a runner/tag)"
tech_debt:
  - phase: "01-structural-foundation-and-test-seam"
    items:
      - "VERIFICATION frontmatter lists FOUND-04/FOUND-05 under requirements_partial while the body and REQUIREMENTS.md mark them Complete (scope was user-confirmed)"
  - phase: "03-transport-hardening-and-concurrency"
    items:
      - "VALIDATION.md is still draft (nyquist_compliant: false); automated evidence exists in 03-VERIFICATION.md"
      - "IssueCertificate's OpenSSL subprocess is not bounded by the request timeout (documented in 03-REVIEW.md / RELEASE-NOTES)"
  - phase: "04-format-lint-normalization-and-ci-gate"
    items:
      - "VALIDATION.md is still draft (nyquist_compliant: false)"
      - "The Linux `test` CI job lists test files explicitly and predates phase 5/6 additions (log_redaction, protocol_version, restricted_methods); they are covered by `test-full`"
      - "x86_64-pc-windows-gnu cross-check not installed; i686 (shipping target) passes"
  - phase: "05-windows-service-reliability"
    items:
      - "VALIDATION.md is still draft (nyquist_compliant: false)"
      - "PowerShell lifecycle scripts unexecuted on this host (Windows VM item)"
  - phase: "06-observability-diagnostics-and-release-verification"
    items:
      - "VALIDATION.md is still draft (nyquist_compliant: false)"
      - "External logger crate unavailable (registry unreachable) — logging implemented in-repo; documented deviation"
      - "Timestamps are epoch seconds, not ISO-8601"
      - "HTTP demo can bind non-loopback in console mode (documented boundary)"
---

# Milestone v0.3.0 — Audit

**Definition of done (from ROADMAP):** harden the SKF gateway into a production
grade service — session-scoped authorization, opaque handles, bounded transport,
deterministic release, a real merge gate, an honest Windows service, and
non-sensitive diagnostics — without breaking the v0.2.0 client contract.

**Verdict:** `tech_debt`. All 44 requirements are satisfied with automated
evidence, all six phases are `passed`, cross-phase wiring is intact, and the frozen
37-fixture contract still matches. No critical gap blocks completion. The remaining
items are documented deferrals and two runtime verifications that need a Windows
host or a CI runner.

## Requirements coverage (3-source cross-reference)

Every requirement is `[x]` in `REQUIREMENTS.md`, listed in a phase `SUMMARY.md`
frontmatter, and covered by a phase `VERIFICATION.md`.

| Phase | Requirements | VERIFICATION | SUMMARY | Traceability | Final |
|-------|--------------|--------------|---------|--------------|-------|
| 1 | FOUND-01..06 | passed (FOUND-04/05 scope-confirmed) | listed | Complete | satisfied |
| 2 | SESS-01..06, RES-01..05 | passed | listed | Complete | satisfied |
| 3 | TRANS-01..07 | passed | listed | Complete | satisfied |
| 4 | QUAL-01..04 | passed | listed | Complete | satisfied |
| 5 | SVC-01..05 | passed | listed | Complete | satisfied |
| 6 | OBS-01..04, REL-01..05, DOC-01..03 | passed | listed | Complete | satisfied |

Total: **44/44 satisfied**. No orphaned requirements (every traceability row
appears in a phase VERIFICATION).

## Phase status

| Phase | Status | Notes |
|-------|--------|-------|
| 1 Structural Foundation | passed | 42 tests; fixtures frozen |
| 2 Session Authorization | passed | 4/4 plans; RES-04 proven by 12 error-path tests |
| 3 Transport Hardening | passed | 4/4 plans; frame/payload/connection/timeout/lock |
| 4 Format/Lint + CI | passed | 3/3 plans; clippy 0, CI six jobs, gate self-test |
| 5 Windows Service | passed | 2/2 plans; startup staging + installer ACL |
| 6 Observability/Release | passed | 3/3 plans; logs, diagnose, protocol version, checksum, docs |

## Cross-phase integration

- `protocol` → `server`/`domain`/`main`; `session` → `main`/`domain`;
  `provider` → `domain`/`server`/`main`/`diagnostic`.
- `service_state` → `win_service` + `diagnostic`; `logging` → `main` + `win_service`;
  `diagnostic` → `main`; `classification::restricted_rejection` → `main::handle_request`.
- `bind_with_progress` (phase 5) is used by `win_service`; `bind` still wraps it for
  `run_server`, so the phase-3 loopback gate and phase-2 contract remain one path.
- The phase-6 `GetProtocolVersion` addition was propagated to
  `CLASSIFIED_METHODS` (TRANS-07) and `ADDITIVE_METHODS` (contract test).
- No dangling imports or unused public modules; `cargo check --all-targets` is clean.

## E2E flows

| Flow | Status | Evidence |
|------|--------|----------|
| Connect → session → CheckPIN → authorized operation | Automated | session/auth + domain tests, session isolation |
| Disconnect / native error → deterministic release | Automated | 12 release-on-error tests, session lifecycle |
| Bounded transport (oversize frame/payload, connection cap, timeout) | Automated | transport_limits, ffi_serialization |
| Windows install → run → upgrade → uninstall → reinstall | **Human (Windows VM)** | `packaging/windows/verify-service.ps1` |
| Tag → build ZIP → checksum → extract-verify → publish | **Human (CI runner)** | `release-windows.yml` |

## Nyquist coverage

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 1 | present | true | — |
| 2 | present | true | — |
| 3 | present | false (draft) | optionally `$gsd-validate-phase 3` |
| 4 | present | false (draft) | optionally `$gsd-validate-phase 4` |
| 5 | present | false (draft) | optionally `$gsd-validate-phase 5` |
| 6 | present | false (draft) | optionally `$gsd-validate-phase 6` |

Discovery only. The phase `VERIFICATION.md` files carry the actual automated
evidence; the draft VALIDATION frontmatter is a bookkeeping lag.

## Test and gate evidence

- `cargo test` → **164 passed, 0 failed**
- `cargo test --test contract_fixtures` → **37/37**
- `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`,
  `cargo check --target i686-pc-windows-gnu` → all clean
- Both GitHub workflow YAML files parse; exactly one `unsafe impl Send`/`Sync`

## Deferred / human items

Recorded in `05-HUMAN-UAT.md` (6) and `06-HUMAN-UAT.md` (4), plus phase-2/3 human
items. None is a requirement gap; they need hardware or a runner.

## Recommendation

Requirements are fully met. Complete the milestone and track the tech debt in the
backlog, or run `$gsd-validate-phase` for phases 3–6 first if the Nyquist
bookkeeping matters for release notes.
