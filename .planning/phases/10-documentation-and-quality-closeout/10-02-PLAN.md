---
plan: 10-02
phase: 10
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - docs/CI.md
  - .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md
autonomous: true
requirements: [QA-01]
---

# Plan 10-02: CI Fast-Job Coverage and Nyquist Sign-Off

<objective>
把阶段 7–9 新增的无硬件测试套件纳入 Linux CI 快速任务，并补做阶段 3–6 的 Nyquist 签核（记录为可执行证据），使采样与自动化覆盖闭环（QA-01）。
</objective>

<threat_model>
**ASVS Level:** 1 · **Block on:** medium

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-10-04 | 新套件不进 CI，回归只能在本地发现 | MEDIUM | 加入 `test` job 并同步 `docs/CI.md` |
| T-10-05 | Nyquist 签核流于形式 | MEDIUM | 审计文档要求每阶段给出可执行命令与实测结果 |
| T-10-06 | 把硬件相关套件误加入 hosted job 造成假红 | MEDIUM | 仅加入 hermetic 套件；`contract_fixtures` 保持本地 |
</threat_model>

<must_haves>
- `.github/workflows/ci.yml` 的 `test` job 增加 `log_redaction`、`protocol_version`、`restricted_methods`、`tls`、`client_auth`、`audit`
- `docs/CI.md` 的 job 表与命令清单同步
- 新增 `10-NYQUIST-AUDIT.md`：逐阶段（03–06）列出任务→当前自动化命令→实测结果，并给出签核结论
- 不修改 `.planning/milestones/` 下的归档文件（冻结历史）
</must_haves>

<tasks>

<task id="10-02-01" type="execute">
  <name>CI 快速任务补齐 hermetic 套件</name>
  <files>.github/workflows/ci.yml, docs/CI.md</files>
  <read_first>
    - `.github/workflows/ci.yml` 的 `test` job
    - `docs/CI.md` 的 Jobs 表
  </read_first>
  <action>
    1. 在 `test` job 的 "Integration suites that do not need the vendor library"
       步骤中追加：
       ```
       cargo test --test session_isolation --test session_lifecycle --test transport_limits --test ffi_serialization --test loopback_gate --test protocol_version --test restricted_methods --test log_redaction
       ```
       并新增一个步骤（阶段 7–9 的 hermetic 安全套件）：
       ```
       - name: TLS and client-authentication suites (hermetic, no token)
         run: cargo test --test tls --test client_auth --test audit
       ```
       仅在确认为无硬件依赖后加入；`contract_fixtures` 保持不加入。
    2. `docs/CI.md`：更新 `test` 行的描述与命令清单，说明新增套件为 hermetic（rcgen 生成证书、无令牌），并保留 `contract_fixtures` 为本地/预发布检查的说明。
  </action>
  <acceptance_criteria>
    - `grep -c 'test client_auth' .github/workflows/ci.yml` 输出 `>= 1`
    - `grep -c 'test tls' .github/workflows/ci.yml` 输出 `>= 1`
    - `grep -c 'test audit' .github/workflows/ci.yml` 输出 `>= 1`
    - `grep -c 'test log_redaction' .github/workflows/ci.yml` 输出 `>= 1`
    - `grep -c 'client_auth' docs/CI.md` 输出 `>= 1`
  </acceptance_criteria>
  <verify>`grep -E 'test (tls|client_auth|audit|log_redaction)' .github/workflows/ci.yml`</verify>
</task>

<task id="10-02-02" type="execute">
  <name>阶段 3–6 的 Nyquist 签核</name>
  <files>.planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md</files>
  <read_first>
    - `.planning/milestones/v0.3.0-phases/03-transport-hardening-and-concurrency/03-VALIDATION.md`
    - `.planning/milestones/v0.3.0-phases/04-format-lint-normalization-and-ci-gate/04-VALIDATION.md`
    - `.planning/milestones/v0.3.0-phases/05-windows-service-reliability/05-VALIDATION.md`
    - `.planning/milestones/v0.3.0-phases/06-observability-diagnostics-and-release-verification/06-VALIDATION.md`
    - 各阶段的 `0X-VERIFICATION.md`
  </read_first>
  <action>
    1. 新建 `10-NYQUIST-AUDIT.md`：
       - 开头说明审计目的与判据（每个任务必须有可执行的自动化验证；无连续 3 个任务缺少验证；采样率与反馈延迟合理）。
       - 对阶段 03、04、05、06 各一节，逐任务列出：任务 ID、验证命令、当前结果（以本仓库当前套件实测为准）。
       - 运行以下命令并记录结果到文档：
         ```
         cargo test --test transport_limits
         cargo test --test log_redaction
         cargo fmt --all -- --check
         cargo clippy --all-targets -- -D warnings
         cargo check --target i686-pc-windows-gnu
         cargo test --test fixture_oracle --test classification_invariants
         ```
       - 给出每阶段的签核结论（COMPLIANT / PARTIAL + 理由）。
       - 明确：归档的 `0X-VALIDATION.md` 保持不动，本文件为签核记录（D-01）。
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md` 为真
    - `grep -c 'Phase 03\|Phase 3' .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md` 输出 `>= 1`
    - `grep -c 'Phase 04\|Phase 4' .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md` 输出 `>= 1`
    - `grep -c 'Phase 05\|Phase 5' .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md` 输出 `>= 1`
    - `grep -c 'Phase 06\|Phase 6' .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md` 输出 `>= 1`
    - `grep -c 'COMPLIANT' .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md` 输出 `>= 4`
  </acceptance_criteria>
  <verify>`ls .planning/phases/10-*/10-NYQUIST-AUDIT.md && cargo test --test transport_limits --test log_redaction && cargo fmt --all -- --check`</verify>
</task>

</tasks>

<verification>
```bash
grep -E 'test (tls|client_auth|audit|log_redaction)' .github/workflows/ci.yml
test -f .planning/phases/10-documentation-and-quality-closeout/10-NYQUIST-AUDIT.md
```
</verification>

<output_spec>
产出 `10-02-SUMMARY.md`：CI 变更、Nyquist 结论摘要。
</output_spec>
