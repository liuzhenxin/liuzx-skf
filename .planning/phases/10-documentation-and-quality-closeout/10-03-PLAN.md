---
plan: 10-03
phase: 10
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - .planning/phases/10-documentation-and-quality-closeout/10-VERIFICATION.md
autonomous: true
requirements: [QA-02, COMPAT-02, QA-01]
---

# Plan 10-03: Hermetic Proof and Milestone-Level Regression

<objective>
证明 TLS/auth 套件在**无令牌**下可跑（QA-02），并做一次跨阶段的完整回归与文档一致性核对，为 v0.4.0 里程碑审计收口。
</objective>

<threat_model>
**ASVS Level:** 1 · **Block on:** medium

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-10-07 | 安全套件隐性依赖令牌/硬件，CI 上假红或漏测 | MEDIUM | 显式 `env -u SKF_TLS_TOKEN` 运行 |
| T-10-08 | 文档与实现漂移未被发现 | MEDIUM | grep 过时声明 |
</threat_model>

<must_haves>
- `env -u SKF_TLS_TOKEN cargo test --test tls --test client_auth --test audit` 通过
- `cargo test --no-fail-fast` 全绿（含 `contract_fixtures`）
- `cargo fmt`/`clippy`/`i686` 全绿
- 文档无"无 TLS/无认证"等过时声明
- `10-VERIFICATION.md` 记录全部证据
</must_haves>

<tasks>

<task id="10-03-01" type="execute">
  <name>无令牌下的 hermetic 安全套件</name>
  <files>.planning/phases/10-documentation-and-quality-closeout/10-VERIFICATION.md</files>
  <read_first>
    - `tests/tls.rs`, `tests/client_auth.rs`, `tests/audit.rs`
  </read_first>
  <action>
    1. 运行（清除令牌相关环境，模拟无硬件的 CI）：
       ```
       env -u SKF_TLS_TOKEN -u SKF_TLS_TOKEN_FILE -u SKF_TLS_CLIENT_CA cargo test --test tls --test client_auth --test audit
       ```
    2. 记录结果；若失败，修正测试对外部环境的隐性依赖（不得通过跳过或忽略来"修复"）。
    3. 写入 `10-VERIFICATION.md` 的 QA-02 小节。
  </action>
  <acceptance_criteria>
    - `env -u SKF_TLS_TOKEN cargo test --test tls --test client_auth --test audit` 退出码 0
    - `grep -c 'QA-02' .planning/phases/10-documentation-and-quality-closeout/10-VERIFICATION.md` 输出 `>= 1`
  </acceptance_criteria>
  <verify>`env -u SKF_TLS_TOKEN cargo test --test tls --test client_auth --test audit`</verify>
</task>

<task id="10-03-02" type="execute">
  <name>全量回归与文档一致性</name>
  <files>.planning/phases/10-documentation-and-quality-closeout/10-VERIFICATION.md</files>
  <read_first>
    - 阶段 10 的其余产出
  </read_first>
  <action>
    1. 运行并记录：
       ```
       cargo test --no-fail-fast
       cargo fmt --all -- --check
       cargo clippy --all-targets -- -D warnings
       cargo check --target i686-pc-windows-gnu
       ```
    2. 文档一致性 grep：
       - `grep -rn 'No TLS' THREAT-MODEL.md docs/ RELEASE-NOTES.md` 期望无过时声明（允许出现在历史发行说明中时需显式说明）。
       - `grep -c 'client_auth' docs/SESSION-AND-LIMITS.md docs/WINDOWS-SERVICE.md THREAT-MODEL.md` 均 `>= 1`。
    3. 完成 `10-VERIFICATION.md`：四项需求（COMPAT-02、QA-01、QA-02、以及阶段 7–9 的回归）与证据；里程碑级结论。
  </action>
  <acceptance_criteria>
    - `cargo test --no-fail-fast` 退出码 0（含 `contract_fixtures`）
    - `cargo fmt --all -- --check` 与 `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo check --target i686-pc-windows-gnu` 退出码 0
    - `grep -c 'COMPAT-02\|QA-01\|QA-02' .planning/phases/10-documentation-and-quality-closeout/10-VERIFICATION.md` 输出 `>= 3`
  </acceptance_criteria>
  <verify>`cargo test --no-fail-fast && cargo clippy --all-targets -- -D warnings && cargo check --target i686-pc-windows-gnu`</verify>
</task>

</tasks>

<verification>
```bash
env -u SKF_TLS_TOKEN cargo test --test tls --test client_auth --test audit
cargo test --no-fail-fast
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo check --target i686-pc-windows-gnu
```
</verification>

<output_spec>
产出 `10-VERIFICATION.md` 与 `10-03-SUMMARY.md`：无令牌证明、全量回归、文档一致性、v0.4.0 里程碑收口结论。
</output_spec>
